<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id="member_list" resultType="vo.MemberVo">
 		select * from member
 	</select>

	<select id="member_one_idx" parameterType="int" resultType="vo.MemberVo">
	select * from member where mem_idx = #{ mem_idx }
	</select>

  	<select id="member_one_id" parameterType="String" resultType="vo.MemberVo">
	select * from member where mem_id = #{ mem_id }
	</select>
	
	<insert id="member_insert"  parameterType="vo.MemberVo">
	insert into member(mem_name,mem_id,mem_pwd,mem_email,mem_zipcode,mem_addr,mem_ip) values( #{ mem_name } , #{ mem_id } , #{ mem_pwd } , #{ mem_email } , #{ mem_zipcode }, #{ mem_addr }, #{mem_ip})
	</insert>
	
	<update id="member_update" parameterType="vo.MemberVo">
	update member set mem_pwd= #{ mem_pwd },mem_email=#{ mem_email },mem_zipcode= #{ mem_zipcode },mem_addr= #{ mem_addr },mem_ip= #{ mem_ip },mem_grade= #{ mem_grade },mem_modifydate =now() where mem_idx=#{ mem_idx }
	</update>

</mapper>












